import React from "react";

type Props = {};

const LocationsPage = (props: Props) => {
  return (
    <div
      className={
        "text-3xl w-full h-full justify-center place-items-center flex"
      }
    >
      LocationsPage
    </div>
  );
};

export default LocationsPage;
